<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Base gradient -->
    <item>
        <shape>
            <gradient
                android:angle="135"
                android:startColor="#1E3A8A"
                android:centerColor="#1E40AF"
                android:endColor="#2563EB"
                android:type="linear" />
            <corners android:bottomLeftRadius="40dp" android:bottomRightRadius="40dp" />
        </shape>
    </item>
    <!-- Subtle circle pattern (top-right) -->
    <item
        android:top="-60dp"
        android:right="-40dp">
        <shape android:shape="oval">
            <solid android:color="#10FFFFFF" />
            <size android:width="200dp" android:height="200dp" />
        </shape>
    </item>
    <!-- Subtle circle pattern (bottom-left) -->
    <item
        android:bottom="-30dp"
        android:left="-30dp">
        <shape android:shape="oval">
            <solid android:color="#08FFFFFF" />
            <size android:width="140dp" android:height="140dp" />
        </shape>
    </item>
</layer-list>
